import { setUiModeAction } from "@/app/actions/hiring";
import { getUiMode } from "@/lib/preferences";

export default async function SettingsPage() { const mode = await getUiMode(); return <div className="mx-auto max-w-[900px] px-4 py-7 sm:px-7 sm:py-10"><header><p className="text-[10px] font-medium tracking-[.18em] text-muted uppercase">Settings</p><h1 className="mt-2 text-3xl font-medium tracking-[-.045em] sm:text-4xl">表示設定</h1></header><section className="mt-8 border-y py-6"><h2 className="text-sm font-medium">UI Mode</h2><p className="mt-2 text-xs leading-5 text-muted">Simpleは日常採用に必要な画面だけを表示します。Advancedは既存の検索・分析機能をサイドバーにも表示します。</p><form action={setUiModeAction} className="mt-5 space-y-3">{[["simple","Simple（デフォルト）","Home、Candidates、Add Candidates、Hiring Pipelineを中心に表示"],["advanced","Advanced","AI Scout、Visual Search、Discoveryなどを常時表示"]].map(([value,label,description]) => <label key={value} className="flex cursor-pointer items-start gap-3 rounded-xl border bg-surface p-4"><input type="radio" name="ui_mode" value={value} defaultChecked={mode === value} className="mt-1 size-4 accent-[#252522]"/><span><span className="block text-sm font-medium">{label}</span><span className="mt-1 block text-xs text-muted">{description}</span></span></label>)}<button className="mt-2 h-10 rounded-lg bg-foreground px-5 text-sm text-background">設定を保存</button></form></section></div>; }
